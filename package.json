{
  "name": "verdeai-platform",
  "version": "0.1.0",
  "private": true,
  "description": "VerdeAI: AI-Optimized. Earth-Aligned.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @verdeai/shared && npm run build -w @verdeai/api && npm run build -w @verdeai/admin-ui",
    "build:ui": "npm run build -w @verdeai/shared && npm run build -w @verdeai/admin-ui",
    "dev:api": "npm run dev -w @verdeai/api",
    "dev:ui": "npm run dev -w @verdeai/admin-ui"
  },
  "engines": {
    "node": ">=20"
  }
}
